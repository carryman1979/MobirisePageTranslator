<UserControl
    x:Class="MobirisePageTranslator.Shared.TranslationGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MobirisePageTranslator.Shared"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False, Type=local:MobiriseProjectViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Border 
            Margin="4" 
            BorderThickness="1"
            BorderBrush="Black"
            Padding="0"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <local:DataGrid ItemsSource="{Binding Path=CellItems}"/>
        </Border>
    </ScrollViewer>
</UserControl>

