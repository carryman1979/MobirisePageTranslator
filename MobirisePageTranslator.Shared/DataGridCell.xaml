<UserControl
    x:Class="MobirisePageTranslator.Shared.DataGridCell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:datagrid="using:MobirisePageTranslator.Shared.Converter.DataGrid"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <UserControl.Resources>
        <datagrid:IsHeaderToBackgroundColorConverter x:Key="IsHeaderToBackgroundColorConverter" />
        <datagrid:IsHeaderToBorderConverter x:Key="IsHeaderToBorderConverter" />
        <datagrid:IsHeaderToFontWeightConverter x:Key="IsHeaderToFontWeightConverter" />
    </UserControl.Resources>
    <Border
        Margin="0" 
        BorderThickness="{Binding IsHeader, Converter={StaticResource IsHeaderToBorderConverter}}"
        BorderBrush="Black"
        Background="{Binding IsHeader, Converter={StaticResource IsHeaderToBackgroundColorConverter}}"
        Padding="0">
        <TextBlock FontWeight="{Binding IsHeader, Converter={StaticResource IsHeaderToFontWeightConverter}}" FontSize="20" Text="{Binding Content}" />
    </Border>
</UserControl>
